<nav class="mt-7 flex flex-col gap-2 border-t border-admin-rule pt-5 font-sans text-sm">
  <%- if controller_name != 'sessions' %>
    <%= link_to "Log in", new_session_path(resource_name), class: "text-admin-ink-muted underline decoration-admin-rule underline-offset-4 hover:text-admin-accent-strong hover:decoration-admin-accent" %>
  <% end %>

  <%- if devise_mapping.registerable? && controller_name != 'registrations' %>
    <%= link_to "Sign up", new_registration_path(resource_name), class: "text-admin-ink-muted underline decoration-admin-rule underline-offset-4 hover:text-admin-accent-strong hover:decoration-admin-accent" %>
  <% end %>

  <%- if devise_mapping.recoverable? && controller_name != 'passwords' && controller_name != 'registrations' %>
    <%= link_to "Forgot your password?", new_password_path(resource_name), class: "text-admin-ink-muted underline decoration-admin-rule underline-offset-4 hover:text-admin-accent-strong hover:decoration-admin-accent" %>
  <% end %>

  <%- if devise_mapping.confirmable? && controller_name != 'confirmations' %>
    <%= link_to "Didn't receive confirmation instructions?", new_confirmation_path(resource_name), class: "text-admin-ink-muted underline decoration-admin-rule underline-offset-4 hover:text-admin-accent-strong hover:decoration-admin-accent" %>
  <% end %>

  <%- if devise_mapping.lockable? && resource_class.unlock_strategy_enabled?(:email) && controller_name != 'unlocks' %>
    <%= link_to "Didn't receive unlock instructions?", new_unlock_path(resource_name), class: "text-admin-ink-muted underline decoration-admin-rule underline-offset-4 hover:text-admin-accent-strong hover:decoration-admin-accent" %>
  <% end %>

  <%- if devise_mapping.omniauthable? %>
    <%- resource_class.omniauth_providers.each do |provider| %>
      <%= button_to "Sign in with #{OmniAuth::Utils.camelize(provider)}", omniauth_authorize_path(resource_name, provider), data: { turbo: false }, class: "text-left text-admin-ink-muted underline decoration-admin-rule underline-offset-4 hover:text-admin-accent-strong hover:decoration-admin-accent" %>
    <% end %>
  <% end %>
</nav>
